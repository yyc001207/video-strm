# SQLite 数据库文件路径（相对 backend 目录）
SQLITE_PATH=./data/video_strm.db
# OpenList 执行日志目录（相对 backend 目录）
OPENLIST_LOG_DIR=./openlist_logs
# CORS 白名单（本地前端端口）
ALLOWED_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
